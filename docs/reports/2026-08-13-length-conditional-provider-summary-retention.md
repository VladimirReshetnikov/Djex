# Length conditional provider-summary retention (superseded)

Date: 2026-08-13

Status: Superseded by the
[Length occurrence-specific ground constraint-discharge report](2026-08-13-length-ground-constraint-discharge.md).

## Historical checkpoint

This document records the earlier retention-only boundary. At that checkpoint,
`AssumedConstraintConditionalProviderSummary` added the ability to retain an
exact closed source-inventory scheme with a nonempty leading constraint context
and the `AssumedProviderLawConditionalOnConstraintDischarge` classifier. The
legacy `AssumedProviderSummary` constructor and its exact bytes remained
unchanged.

The retention checkpoint deliberately granted no behavioral use: detached
provider evaluation and all candidate paths rejected the conditional law,
associated rows rejected activated obligations, Length did not consume the
package-private resolver, and Z3 supplied no dictionary evidence. Those limits
describe that historical checkpoint only.

## Compatibility carried forward

The finalized boundary preserves the retention identities established here:

- legacy-only provider inventory v2 and semantic inventory v1 remain exact;
- conditional provider inventory v3 and semantic inventory v2 remain exact;
- the conditional provider-summary field contains no new dictionary-uniform
  behavioral marker; and
- the checked summary continues to retain source-derived authority rather than
  a detachable caller claim.

Behavioral use now begins at later identity layers. Conditional Length sessions
select policies 8/9/10, conditional concrete encodings select 4/5/6, and a
ground-discharged associated candidate selects v3. Only the final occurrence in
its own protected certificate chain may use the conditional law after every
activated obligation has independently passed the exact-inventory, alias-free,
forall-free, first-order ground resolver with no query givens. That use assumes
the provider law is uniform over admitted dictionary evidence. Detached
provider evaluation still rejects it, and Z3 remains outside dictionary
authority.

See the superseding report for the current precedence, sanitized diagnostics,
receipt association, prefix audit, identity markers, and validation boundary.
